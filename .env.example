### Environment variables for local development
# Use PUBLIC_ prefix for variables exposed to the browser. Keep secrets in server-only env vars.

# Base URL for the WordPress REST API. For dev, point to the live site.
PUBLIC_WP_API_BASE=https://wp.theinnovator.org/wp-json
